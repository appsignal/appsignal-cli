---
bump: minor
type: add
---

Added `skill install` so you can install the bundled AppSignal CLI skill for OpenCode, Codex, or Claude. The installed skill explains the user-facing CLI commands and LLM-friendly output like `logs search --json`.
